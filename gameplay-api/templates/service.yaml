apiVersion: v1
kind: Service
metadata:
  name: {{ include "gameplay-api.fullname" . }}
  labels:
    {{- include "gameplay-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gameplay-api.selectorLabels" . | nindent 4 }}
