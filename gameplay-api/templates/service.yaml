apiVersion: v1
kind: Service
metadata:
  name: {{ include "gameplay-back.fullname" . }}
  labels:
    {{- include "gameplay-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gameplay-back.selectorLabels" . | nindent 4 }}
