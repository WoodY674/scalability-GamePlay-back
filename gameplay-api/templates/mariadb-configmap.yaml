apiVersion: v1
kind: ConfigMap
metadata:
  name: mariadb-config
data:
  MYSQL_DATABASE: {{ .Values.mariadb.auth.database }}
  MYSQL_USER: {{ .Values.mariadb.auth.username }}
  MYSQL_PASSWORD: {{ .Values.mariadb.auth.password }}
  MYSQL_PORT: "3306"
  MYSQL_HOST: "mariadb"
